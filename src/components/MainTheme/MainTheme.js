import { createTheme } from "@mui/material";

const MainTheme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#373737",
    },
    secondary: {
      main: "#fff",
    },
    warning: {
      main: "#dc143c",
    },
  },
});

export default MainTheme;
