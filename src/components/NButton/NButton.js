import styled from "@emotion/styled";

const ComponentNButton = styled.button`
  color: red;
`;

const NButton = () => {
  return (
    <React.Fragment>
      <span>a</span>
    </React.Fragment>
  );
};

export default NButton;
